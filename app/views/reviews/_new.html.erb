<div class="container border m-1">
<h2>Create a Review</h2>
  <%= simple_form_for [ restaurant, review ] do |r| %>
    <%= r.input :rating, input_html: { min: '0' , max:'5'} %>
    <%= r.input :content %>
    <%= r.button :submit, class: "btn btn-primary" %>
  <% end %>
  <%# <%= link_to "Back", restaurant_reviews_path(restaurant) %>
</div>
